spring:
  profiles:
    active: local
  jackson:
    property-naming-strategy: SNAKE_CASE
  main:
    allow-circular-references: false
  jpa:
    hibernate:
      ddl-auto: none
---
spring:
  config:
    activate:
      on-profile: local
  datasource:
    primary:
      url: jdbc:mysql://localhost:3306/acore_auth
      username: root
      password: Wowlibre96@@
      driver-class-name: com.mysql.cj.jdbc.Driver
    secondary:
      url: jdbc:mysql://localhost:3306/acore_characters
      username: root
      password: Wowlibre96@@
      driver-class-name: com.mysql.cj.jdbc.Driver
    three:
      url: jdbc:mysql://localhost:3306/acore_world
      username: root
      password: Wowlibre96@@
      driver-class-name: com.mysql.cj.jdbc.Driver
server:
  port: 8090
application:
  credentials:
    command:
      username: 'admin' # Username emulator for the command
      password: 'admin' # Password emulator for the command
  account:
    api-key: s2sftjgo750sulq
    username: mschitiva70@gmail.com
    password: 'sebas'
  urls:
    wow-libre-login: http://localhost:8091/core/api/auth/login
    wow-libre-server: http://localhost:8091/core/api/realm/key
  security:
    jwt:
      secret-key: A3F1E4B2D0A728C9F54D8B32C7A59A7D0B9A8F94D1F6C762E7DA56231988C158
      expiration: 604800000  # 7 day
      refresh-token:
        expiration: 1209600000 # 14 días
server-web:
  emulator-type: "AzerothCore"
  domain: "wowlibre.com"
---
spring:
  config:
    activate:
      on-profile: prod
  datasource:
    primary:
      url: ${DB_WOW_CLIENT_HOST_AUTH}
      username: ${DB_WOW_CLIENT_USERNAME}
      password: ${DB_WOW_CLIENT_PASSWORD}
      driver-class-name: com.mysql.cj.jdbc.Driver
    secondary:
      url: ${DB_WOW_CLIENT_HOST_CHARACTERS}
      username: ${DB_WOW_CLIENT_USERNAME}
      password: ${DB_WOW_CLIENT_PASSWORD}
      driver-class-name: com.mysql.cj.jdbc.Driver
    three:
      url: ${DB_WOW_CLIENT_HOST_WORLD}
      username: ${DB_WOW_CLIENT_USERNAME}
      password: ${DB_WOW_CLIENT_PASSWORD}
      driver-class-name: com.mysql.cj.jdbc.Driver
server:
  port: ${SERVER_PORT}
application:
  credentials:
    command:
      username: ${GM_USERNAME}
      password: ${GM_PASSWORD}
  account:
    api-key: ${API_KEY_CORE}
    username: ${USERNAME_CORE}
    password: ${PASSWORD_CORE}
  urls:
    wow-libre-login: ${HOST_BASE_CORE}/core/api/auth/login
    wow-libre-server: ${HOST_BASE_CORE}/core/api/realm/key
  security:
    jwt:
      secret-key: ${SECRET_JWT}
      expiration: 604800000  # 7 day
      refresh-token:
        expiration: 1209600000 # 14 días
server-web:
  emulator-type: ${EMULATOR_TYPE}
  domain: ${DOMAIN}